import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'

// https://vitejs.dev/config/
export default defineConfig({
  plugins: [react()],
  server: {
    // Wenn du lokal 'npm run dev' ausführst, leitet Vite alle /api/ Anfragen 
    // automatisch an dein FastAPI Backend weiter.
    proxy: {
      '/api': {
        target: 'http://127.0.0.1:8000',
        changeOrigin: true,
        secure: false,
      }
    }
  }
})